import { Box } from "@mui/material";
import React, { useEffect, useState, useRef } from "react";

const Index = () => {
  return (
    <Box ml={2} mr={2}>
      Weekday
    </Box>
  );
};

export default Index;
